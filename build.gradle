apply plugin: 'java'
apply plugin: 'maven'

group = 'com.beanstalk.new-project'
version = '0.1'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
    maven { url "file:../maven-repo" }
    jcenter()
    mavenCentral()
}

dependencies {
    compile 'com.google.code.gson:gson:+'
    compile 'junit:junit:+'
}

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: "file:../maven-repo")
        }
    }
}
